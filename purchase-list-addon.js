(function PurchaseListAddon(w,d){'use strict';const MARKER='PURCHASE_LIST_ADDON_READY_V1';const $=id=>d.getElementById(id);const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();const read=(k,f)=>{try{const r=localStorage.getItem(k);return r?JSON.parse(r):f}catch{return f}};const arr=v=>Array.isArray(v)?v:[];function ing(){return arr(read('ingredients',[]))}function findIng(t){const n=norm(t);return ing().find(x=>n.includes(norm(x.name))||norm(x.name).includes(n))